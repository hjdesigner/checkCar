import {
  View,
  Text,
} from 'react-native'
import styled from 'styled-components'

export const Container = styled.View`
  display: flex;
  flex-direction: row;
  padding: 0 24px;
  margin-bottom: 8px;
`
export const TextVehicle = styled.Text`
  color: #000;
  font-size: 17px;
`